<!-- artifact
emoji: 🔴
tasks: p0-w0-t8
stack: Python, httpx, Pydantic
-->

# Pokémon CLI

Fetches the PokéAPI, parses the response into typed Pydantic models, and prints it. A throwaway script to lock in reading a JSON API into typed Python.

## Run

```bash
uv run pokemon-cli/main.py
```
